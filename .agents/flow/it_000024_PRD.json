{
  "goals": [
    "Enable loading ACEStep components from separate model files (diffusion model, text encoder, VAE)",
    "Remove the monolithic checkpoint loading path",
    "Keep `/api/generate` fully functional with no visible change to callers"
  ],
  "userStories": [
    {
      "id": "US-001",
      "title": "Backend loads ACEStep from separate model files",
      "description": "As a backend system, I want to initialise the ACEStep pipeline by loading diffusion model, text encoder, and VAE from separate files so that operators can use individually distributed model weights without needing an AIO checkpoint.",
      "acceptanceCriteria": [
        {
          "id": "US-001-AC01",
          "text": "`audio_repository.py` calls `manager.load_unet(diffusion_model_name)` to load the diffusion model from the `diffusion_models/` folder"
        },
        {
          "id": "US-001-AC02",
          "text": "`audio_repository.py` calls `manager.load_clip(text_encoder_name)` to load the text encoder from the `text_encoders/` folder (normal clip loader, single file, no second checkpoint)"
        },
        {
          "id": "US-001-AC03",
          "text": "`audio_repository.py` calls `manager.load_vae(vae_name)` to load the VAE from the `vae/` folder"
        },
        {
          "id": "US-001-AC04",
          "text": "`AceComfyPipeline` is constructed with the three components loaded above"
        },
        {
          "id": "US-001-AC05",
          "text": "The old `load_checkpoint()` call and its associated `ACE_COMFY_CHECKPOINT` constant are removed"
        },
        {
          "id": "US-001-AC06",
          "text": "Typecheck / lint passes"
        }
      ]
    },
    {
      "id": "US-002",
      "title": "`/api/generate` endpoint continues to work",
      "description": "As a caller of the audio API, I want `POST /api/generate` to return a valid audio stream so that the pipeline change is transparent to the rest of the system.",
      "acceptanceCriteria": [
        {
          "id": "US-002-AC01",
          "text": "`POST /api/generate` returns HTTP 200 with a valid audio stream when all three model env vars are set and model files are present"
        },
        {
          "id": "US-002-AC02",
          "text": "Missing or misconfigured model env vars produce a clear `RuntimeError` on startup or at first request (before inference is attempted)"
        },
        {
          "id": "US-002-AC03",
          "text": "Typecheck / lint passes"
        }
      ]
    },
    {
      "id": "US-003",
      "title": "Separate model paths are configurable via environment variables",
      "description": "As an operator, I want to specify each model file name via an environment variable so that I can point the backend to whichever model files I have downloaded.",
      "acceptanceCriteria": [
        {
          "id": "US-003-AC01",
          "text": "`constants.py` exposes `ACE_COMFY_DIFFUSION_MODEL` (filename inside `diffusion_models/`)"
        },
        {
          "id": "US-003-AC02",
          "text": "`constants.py` exposes `ACE_COMFY_TEXT_ENCODER` (filename inside `text_encoders/`)"
        },
        {
          "id": "US-003-AC03",
          "text": "`constants.py` exposes `ACE_COMFY_VAE` (filename inside `vae/`)"
        },
        {
          "id": "US-003-AC04",
          "text": "`ACE_COMFY_CHECKPOINT` is removed from `constants.py`"
        },
        {
          "id": "US-003-AC05",
          "text": "`backend/.env.example` is updated to document the three new env vars and remove the checkpoint var"
        },
        {
          "id": "US-003-AC06",
          "text": "Typecheck / lint passes"
        }
      ]
    }
  ],
  "functionalRequirements": [
    {
      "id": "FR-1",
      "description": "** Remove `ACE_COMFY_CHECKPOINT` from `constants.py` and `.env.example`; add `ACE_COMFY_DIFFUSION_MODEL`, `ACE_COMFY_TEXT_ENCODER`, and `ACE_COMFY_VAE`"
    },
    {
      "id": "FR-2",
      "description": "** In `audio_repository.py`, replace the `load_checkpoint()` call with sequential calls to `manager.load_unet()`, `manager.load_clip()`, and `manager.load_vae()` using the new constants"
    },
    {
      "id": "FR-3",
      "description": "** The pipeline cache (`_cached_pipeline`) continues to work — models are loaded once on first request and reused"
    },
    {
      "id": "FR-4",
      "description": "** If any required env var is empty, raise a `RuntimeError` with a descriptive message before attempting to load models"
    }
  ]
}
